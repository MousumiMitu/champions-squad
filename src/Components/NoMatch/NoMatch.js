import React from "react";
import Header from "../Header/Header";

const NoMatch = () => {
  return (
    <div>
      <Header></Header>
      <h2 className="text-center">Sorry! Nothing match the search detail...</h2>
    </div>
  );
};

export default NoMatch;
